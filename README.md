# meal-companion

## Run Application

### In DEVELOPMENT mode
To init database before application starts run `flask init-db`

Setup eniroment:
Windows PowerShell
```
$env:FLASK_APP = "main"
$env:FLASK_ENV = "development"
```

Than run `flask run`
