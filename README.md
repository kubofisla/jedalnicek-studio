# meal-companion

## Run Application

### DEBUG
To init Db before application start run `flask init-db`

Setup eniroment
```
$env:FLASK_APP = "main"
$env:FLASK_ENV = "development"
```

Than run `flask run`
